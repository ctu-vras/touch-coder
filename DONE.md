# DONE

Completed work, moved here from `TODO.md`. Each entry keeps its original one-liner
with a `**Completed:** YYYY-MM-DD — <outcome>` line prepended. Fix plans live in
`docs/reviews/<date>/done/`.

## Code Review — 2026-07-12

- **H2** Per-edit O(N) timeline rebuild stalls long videos in 3D mode. — plan: [fix_H2.md](docs/reviews/2026-07-12/done/fix_H2.md)
  **Completed:** 2026-07-13 — Extracted the cumulative pose-timeline calculation into pure full-build and in-place suffix-update functions, replaced per-edit whole-cache invalidation with earliest-dirty-frame tracking, and made body/head scale writes invalidate the correct suffix. Added deterministic equivalence, scan-bound, and cache-preservation tests in `tests/test_timeline_incremental_H2.py`. The optional partial-raster repaint was deferred; state reconstruction is now proportional to the affected suffix. Export schema impact: none.
- **H3** "Incremental" saves are full re-read + full rewrite every time. — plan: [fix_H3.md](docs/reviews/2026-07-12/done/fix_H3.md)
  **Completed:** 2026-07-13 — Replaced touch and pose unified read/merge/rewrite saves with durable append-only journals using fixed column order, last-writer-wins loading, and atomic load-time compaction above a 2× duplicate threshold. Full frozen-schema exports now run from a deep snapshot on a worker thread behind a responsive modal progress window; live bundles changed during export remain dirty. H3 supersedes C4's former pose read-error guard because saves no longer reread the unified file. Added `tests/test_incremental_save_H3.py` and updated the C4 preservation guard. Export schema impact: none.
- **M9** `on_close` logic inverted; Cancel leaves a half-dead app. — plan: [fix_M9.md](docs/reviews/2026-07-12/done/fix_M9.md)
  **Completed:** 2026-07-13 — Converted the close dialog into a boolean modal, moved confirmation ahead of every save/teardown side effect, made `save_data` return success, and added an explicit close-anyway decision for save failures. Confirmed closes save position/time, shut down the loader pool, and destroy the root in order; Cancel leaves the session and pool intact. Added `tests/test_close_m9.py` plus a real Tk modal/worker-boundary smoke. Export schema impact: none.
- **C1** No atomic writes → a crash mid-save corrupts the source-of-truth CSV. — plan: [fix_C1.md](docs/reviews/2026-07-12/done/fix_C1.md)
  **Completed:** 2026-07-12 — Added stdlib `src/atomic_io.py` (`atomic_write`: temp → fsync → `os.replace`, with opt-in `.bak`) and routed all 9 live writers (unified/pose CSV, both exports, export + video-time metadata, config, last-position, clothes) through it; interrupted saves now leave the previous file intact. Export schema impact: none.
- **C2** Global key bindings (arrows/space/`d`) leak into the Note entry → destroys annotations. — plan: [fix_C2.md](docs/reviews/2026-07-12/done/fix_C2.md)
  **Completed:** 2026-07-12 — Added a stateless focus guard: new `LabelingApp._entry_has_focus()` predicate + a `_guard_key()` wrapper on all six toplevel nav key bindings in `_bind_navigation` (arrows / Shift-arrows / space / `d`), so keystrokes typed into the Note entry no longer toggle playback, jump frames, or delete dots — the char still lands in the entry, only the nav side effect is skipped. Guard sits at the binding site so `<Button-2>` middle-mouse dot removal (also `on_middle_click`) is unaffected. Deleted the dead `disable_arrow_keys`/`enable_arrow_keys` methods and the redundant `enable_arrow_keys()` call in `save_note`. Export schema impact: none.
- **C4** `save_pose_dataset` silently discards all prior rows on a read error (active mode). — plan: [fix_C4.md](docs/reviews/2026-07-12/done/fix_C4.md)
  **Completed:** 2026-07-12 — Narrowed the existing-file re-read guard in `pose_mismatch_data.save_pose_dataset`: added a dedicated `PoseUnifiedReadError`, kept `pd.errors.EmptyDataError` as the benign "no prior rows" case (logs DEBUG, proceeds with an empty map), and made any genuine parse error log an `ERROR` (rule 0) and raise `PoseUnifiedReadError` instead of resetting `existing_map = {}` and overwriting the live file with only this session's rows. Wired the caller `LabelingApp.save_data` to catch it, `traceback.print_exc()` + `messagebox.showerror`, and return early so the export rewrite and `Changed`-flag clear are skipped — the on-disk file is left byte-for-byte untouched and dirty frames stay dirty for a clean retry. Final write still routes through C1's `atomic_write`. New regression guard `tests/test_pose_save_no_silent_loss.py`. Export schema impact: none.
- **H4** Config loaders (all but `load_config`) crash the app on a corrupt `config.json`. — plan: [fix_H4.md](docs/reviews/2026-07-12/done/fix_H4.md)
  **Completed:** 2026-07-12 — Funneled all six live loaders (`load_config_flags`, `load_perf_config`, `load_video_downscale`, `load_jump_seconds`, `load_realtime_arrow_hold`, `load_parameter_names_into`) plus the dead `load_display_limits` through the hardened `load_config()` in `config_utils.py`, replacing every bare `_ensure_config_file()`+`open`+`json.load` prologue with `config = load_config()`; added a single `CONFIG_DEFAULTS` dict as the one source of truth for defaults, hardened `load_config` to coerce non-dict JSON to `{}` and to print a `WARNING:` on any read/parse failure. A corrupt/truncated `config.json` now yields `{}` once and every loader falls back to its documented default instead of raising `JSONDecodeError` on startup. No caller/return-shape changes. Export schema impact: none.
- **H6** `LimbView`: create-on-read mutation + dead `UserDict` backing store. — plan: [fix_H6.md](docs/reviews/2026-07-12/done/fix_H6.md)
  **Completed:** 2026-07-12 — Rewrote `video_model.LimbView` from a `UserDict` subclass into a thin plain accessor that holds a back-reference to the owning `Video` and resolves `frames` lazily via a `_frames` property. `__getitem__` now reads without inserting (raises `KeyError` on a missing frame instead of the old `setdefault` create-on-read); `.get()` stays non-mutating; the buggy always-truthy `setdefault` is dropped; and `__len__/__iter__/__contains__/keys/values/items` now delegate to the live backing `frames` dict instead of the dead, always-empty `UserDict.data`. Constructor changed to `LimbView(video, limb)` (only caller is `Video.__init__`), and the 8 manual `dataRH._frames = ...` rebind lines in `labeling_app.load_video` were deleted since the views now track `video.frames` automatically. New regression guard `tests/test_limbview_h6.py`. Export schema impact: none.
- **H5** OpenCV frame-extraction fallback has no failure signal. — plan: [fix_H5.md](docs/reviews/2026-07-12/done/fix_H5.md)
  **Completed:** 2026-07-12 — Gave frame extraction a failure contract: new `FrameExtractionError` in `frame_utils.py`; `create_frames` now recounts via `_count_jpg_files` after extraction so BOTH the ffmpeg and OpenCV paths funnel through one guard, logs `ERROR:` and raises on a genuinely empty `frames/` folder (reliability-copy branch guarded too), and returns the produced frame count on success (`_extract_frames_opencv` now `return`s its count). `labeling_app.load_video` catches it, logs `ERROR:`, shows a "Frame Extraction Failed" `messagebox.showerror`, resets the labeling-time timer via the new `_stop_video_timer_if_any()` (started before extraction), and returns before `display_first_frame()` / the buffer thread — so an unreadable/unsupported-codec video aborts cleanly instead of opening onto blank frames. Below-tolerance partial extraction stays a WARN (unchanged). Export schema impact: none.
- **M4** Division by zero when FPS probe returns 0. — plan: [fix_M4.md](docs/reviews/2026-07-12/done/fix_M4.md)
  **Completed:** 2026-07-12 — Guarded the touch exporter's timestamp in `data_utils.export_from_unified` (`Time_ms = (f / frame_rate) * 1000.0 if frame_rate else 0.0`), matching `export_pose_dataset`'s existing guard — a 0-FPS probe no longer raises `ZeroDivisionError` mid-Save (which previously left the unified CSV updated but the export stale). Also guarded the sibling GUI site `labeling_app.update_frame_counter` (time label shows `--:-- / --:--` when fps is unknown instead of crashing on every frame display; goes slightly beyond the plan's minimal scope since a 0-fps video would otherwise still be unusable). `Video.get_total_frames`'s `fps:.3f` print needs no change (`cap.get` returns a float `0.0`, never `None`). Pre-written red test `tests/test_export_zero_fps.py` now green; schema-lock tests unchanged. Export schema impact: none (value fallback on an existing column only).
- **M2** `mark_bundle_changed(index=None)` ignores its argument. — plan: [fix_M2.md](docs/reviews/2026-07-12/done/fix_M2.md) — landed **before H2**
  **Completed:** 2026-07-13 — Updated `mark_bundle_changed` and `notify_bundle_changed` to honor an explicit frame index while retaining the current-frame default via an `is None` check, including valid frame `0`. Added `tests/test_mark_bundle_changed_m2.py` to pin explicit, zero, and default index behavior. Export schema impact: none.
- **M5** `_swap_lr_in_string` corrupts free text (dead-but-dangerous). — plan: [fix_M5.md](docs/reviews/2026-07-12/done/fix_M5.md) — landed **before M6**
  **Completed:** 2026-07-13 — Deleted the unreachable legacy `merge_and_flip_export` pipeline, `_swap_lr_in_string`, `_swap_lr_columns`, `_prepend_header`, its stale commented caller block/docstring text, and the unused import from `labeling_app.py`. The live atomic touch and pose exporters remain unchanged. Export schema impact: none.
- **M6** Missing `encoding="utf-8"` on builtin `open()` calls. — plan: [fix_M6.md](docs/reviews/2026-07-12/done/fix_M6.md)
  **Completed:** 2026-07-13 — Added explicit UTF-8 encoding to every remaining builtin text-mode `open()` across the legacy CSV helpers, config loader, clothes/position readers, and analysis master HTML writer. Moved legacy notes loading into `data_utils.load_notes_csv`, which reads UTF-8 first and visibly retries cp1252 for files created by older Windows builds; `LabelingApp.load_video` now uses that helper. Escaped the video name in the analysis `<title>` and `<h1>`. Added `tests/test_m6_utf8.py` for UTF-8 config, UTF-8/cp1252 notes, and limb-parameter round-trips. Export schema impact: none.
- **H1** Tkinter called from background threads (playback/buffer) → sporadic crashes. — plan: [fix_H1.md](docs/reviews/2026-07-12/done/fix_H1.md)
  **Completed:** 2026-07-12 — Enforced the thread → UI boundary for both daemon threads in `labeling_app.py` (approach B). The playback thread's tick in `background_update_play` no longer calls `next_frame(direction, play=True)` (which reached `display_first_frame`'s `ImageTk.PhotoImage`/`frame_label.configure` and `draw_timeline2`'s canvas ops off-thread): it now only advances plain state (`video.current_frame`, `_last_step_sign`), pokes `_priority_event` if the destination got evicted, and schedules ONE main-thread redraw via `self.after(0, _render_current_frame)` — a new UI-thread-only method (`display_first_frame()` + `draw_timeline2()`, teardown-guarded: no-ops with a DEBUG log when `video is None` or on `TclError`). A `_render_pending` debounce flag caps the queue at one pending redraw so scheduled paints can't pile up faster than the UI thread drains them; the `1/frame_rate` sleep pacing is preserved verbatim. The boundary/next-frame decision was extracted into the pure `_compute_play_step` (unit-tested). The buffer thread's off-thread `winfo_width/height` reads in `background_update` were replaced with `_display_w/_display_h` caches maintained on the main thread (`on_resize` `<Configure>` handler + seeded in `load_video` before the thread starts). Added a dev guard — `_is_ui_thread`/`_assert_ui_thread` (gated by `DEBUG_ASSERT_UI_THREAD`, off by default) called at the top of `display_first_frame`/`draw_timeline`/`draw_timeline2`/`render_pose_canvas` — so future off-thread widget access fails loudly at the call site; `after()` scheduling from the worker is wrapped to stop playback with a DEBUG log if Tk is mid-shutdown. `next_frame`/`display_first_frame` internals and all main-thread navigation paths unchanged. New regression guard `tests/test_h1_thread_boundary.py`. Export schema impact: none.
- **Remove Sort Frames** (unused feature; resolves **C3** as a side effect).
  **Completed:** 2026-07-12 — Deleted `src/sort_frames.py` (both the live `process_touch_data_strict_transitions` path — broken twice over per C3 — and the dead `datavyu_*` variant). Removed the "Sort Frames" button from `ui_components.py`, the `sort_frames()` method and `process_touch_data_strict_transitions` import from `labeling_app.py`, and renamed `_set_sort_analysis_state` → `_set_mode_button_states` (it still gates the Analysis + Clothes buttons by mode; only the sort part was removed). Dropped the `sorted_frames/` tree, workflow step 6, and module-list references from PROJECT.md and `sort_frames.py` from HANDOFF.md's core-module list. `.gitignore`'s `sorted_frames/` lines kept so previously generated output dirs stay ignored. Sort Frames only *read* the export CSV — export schema impact: none.
- **M1** `toggle_limb_parameter` stores the string `"None"` instead of `None`. — plan: [fix_M1.md](docs/reviews/2026-07-12/done/fix_M1.md)
  **Completed:** 2026-07-13 — Reused `_param_next_state` for limb toggles, normalized the legacy `"None"` artifact in unified/export/legacy limb-parameter loaders, and defensively canonicalized it at export so cleared values serialize as JSON `null` and flat-export empty cells. Added `tests/test_limb_param_none.py`. Export schema impact: none; authorized existing-cell value correction only.
- **M3** `_Look`/gaze data is never exported (schema mismatch). — plan: [fix_M3.md](docs/reviews/2026-07-12/done/fix_M3.md) — decided: vestigial, remove + coworker notice in `docs/EXPORT_NOTES.md`
  **Completed:** 2026-07-13 — Removed the vestigial per-limb `Look` field from the live data model, defaults, click/delete handlers, export-recovery plumbing, debug summaries, and legacy-reader output while retaining passive compatibility with old unified JSON blobs. Documented the external-pipeline migration to global `Parameter_1` in `docs/EXPORT_NOTES.md` and linked it from PROJECT.md. Added `tests/test_look_vestigial.py`. Export schema impact: none.
- **M10** `parse_xy` silently drops non-digit coordinates → X/Y/Zones desync. — plan: [fix_M10.md](docs/reviews/2026-07-12/done/fix_M10.md) — batch with M1+M3 (same function)
  **Completed:** 2026-07-13 — Replaced independent digit-only parsing with tolerant pairwise X/Y recovery that accepts scalar, float, and negative coordinates; drops malformed or unmatched pairs together with their zone bucket; pads missing zones; and logs contextual warnings plus an aggregate drop count. Added `tests/test_import_export_xy.py`. Export schema impact: none.
- **M8** `load_pose_dataset` uses the slow `iterrows()` path. — plan: [fix_M8.md](docs/reviews/2026-07-12/done/fix_M8.md)
  **Completed:** 2026-07-13 — Converted only the active pose load loop to positional `itertuples(index=False, name=None)` with a column-index map and missing-column defaults, preserving the existing per-field fallback semantics while adding missing-`Frame` and completion/timing logs. Added `tests/test_pose_load_itertuples.py`; the synthetic 50,000-row load improved from 6.533s to 1.743s. Export schema impact: none.
- **M7** Resource leaks + O(n²) polling in frame extraction. — plan: [fix_M7.md](docs/reviews/2026-07-12/done/fix_M7.md) — explains the "crash on close during frame generation" TODO note (resource half here, teardown half in M9); open decision D1 (close-during-extraction UX)
  **Completed:** 2026-07-13 — Hardened all extraction resources: OpenCV captures now release in `finally`, ffmpeg is killed/reaped and its pipes/drainers are closed on every exceptional exit, `imwrite(False)` is a typed hard failure, and unexpected extractor exceptions funnel through `FrameExtractionError`. Replaced repeated directory scans with an amortized-O(n) sequential probe; Reliability copy now filters non-frames, logs skips, wraps copy failures, and throttles progress with a guaranteed final update. D1 remains intentionally owned by M9: closing mid-generation now cleans up resources but GUI teardown ordering is unchanged. Added `tests/test_frame_utils_m7.py`. Export schema impact: none.
- **M11** Pose `ScaleFactor` not clamped on load. — plan: [fix_M11.md](docs/reviews/2026-07-12/done/fix_M11.md)
  **Completed:** 2026-07-13 — Added NaN-neutral scale conversion and centralized in-place normalization in `ensure_pose_bundle` for body/head raw and factor values. Invalid/NaN values become neutral or derive from sanitized raw values, finite values clamp to `[0.7, 1.3]`, and every actual correction is warning-logged without changing `ScaleSet`. Added `tests/test_pose_scale_clamp.py`. Export schema impact: none structurally; corrupt existing scale cells are normalized into the documented range.
- **M12** Analysis: error-swallowing reader + lossy transition metrics. — plan: [fix_M12.md](docs/reviews/2026-07-12/done/fix_M12.md) — decided: pairwise cartesian (multiple transitions per multi-zone touch)
  **Completed:** 2026-07-13 — Added typed/logged current+legacy export reading with immediate OSError propagation and chained `ExportReadError`, surfaced all analysis callback failures in a messagebox, and logged the remaining analysis fallbacks. Removed fabricated transitions for touches still open at the final frame and implemented the decided cartesian start-zone × end-zone counts with a heatmap footnote; an observed OFF with no zones retains the planned `NN` endpoint default. Added `tests/test_analysis_m12.py`. Export schema impact: none.
