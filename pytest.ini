[pytest]
testpaths = tests
norecursedirs = .git .venv build dist scripts Labeled_data icons assets docs
markers =
    gui: tests that require a display and a real Tk root (run with -m gui)
addopts = -m "not gui"
